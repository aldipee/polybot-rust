# good_open_paired_seed

Certifies a healthy open-path replay in paper mode: the paired startup seed posts, both sides fill, the runtime transitions through `both_sides_live`, and the bot finishes back in normal trading control.
